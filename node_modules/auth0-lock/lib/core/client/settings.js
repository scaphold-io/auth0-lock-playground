"use strict";

exports.__esModule = true;
exports.fetchClientSettings = fetchClientSettings;

var _cdn_utils = require("../../utils/cdn_utils");

function fetchClientSettings(clientID, assetsUrl, cb) {
  (0, _cdn_utils.load)({
    method: "setClient",
    url: assetsUrl + "/client/" + clientID + ".js?t" + +new Date(),
    check: function check(o) {
      return o && o.id === clientID;
    },
    cb: cb
  });
}
